# PTP-GOV.4.6A — Contratos e caracterização visual

Data: 2026-07-13

Base: `b4a058fdb541ddde3ada16a6de8cac04ab29a300`

Status: `PASS_FINAL_VISUAL_CONTRACTS_AND_FAIL_CLOSED_CHARACTERIZATION`

## Resultado

A etapa congelou o contrato da janela autorizada e o comportamento de falha fechada sem
capturar pixels, executar OCR, abrir corretora, iniciar servidor ou acessar runtime real.

Foram usados exclusivamente mocks, objetos controlados e PNGs sintéticos dentro de
`TemporaryDirectory`.

## Preflight gráfico

O computador está em sessão X11, desktop XFCE, gerenciador `Xfwm4` e geometria raiz
`1366x768+0+0`.

Ferramentas disponíveis:

- `wmctrl`;
- `xprop`;
- `xwininfo`;
- `xwd`.

As três primeiras consultaram apenas metadados do root X11. `xwd` declara seleção por ID
explícito, mas não capturou nenhuma janela nesta etapa. Sobreposição e comportamento do
compositor não são comprováveis apenas com esses metadados; a regra vinculante é falhar
fechada.

## Contrato congelado

Cada captura futura exigirá ID, PID, processo, padrão integral de título, geometria,
display server, visibilidade, estado não minimizado, foreground quando necessário,
geometria estável e confirmação explícita da fonte.

Também são obrigatórios monitor único, destino por ID explícito e proibição de fallback
para tela inteira, desktop ou janela ativa genérica.

O contrato completo está em
`docs/contracts/PTP-GOV.4.6A_AUTHORIZED_WINDOW_CONTRACT.md`.

## Falha fechada

Qualquer divergência produz:

```text
CAPTURE_ALLOWED=NO
CAPTURE_EXECUTED=NO
OCR_EXECUTED=NO
INVALID_READING_PUBLISHED=NO
LAST_VALID_READING_PRESERVED=YES
SOURCE_STATE=WAITING_SOURCE_OR_SOURCE_NOT_CONFIRMED
```

VS Code, Codex e terminais são sempre rejeitados. Navegadores somente podem hospedar a
fonte quando ID, PID, processo, título e geometria coincidirem exatamente com o contrato.

## Testes

```text
VISUAL_CONTRACT_TESTS_PASS=14
PREVIOUS_AUTOMATED_TESTS_PASS=70
TOTAL_TESTS_PASS=84
TOTAL_TESTS_FAIL=0
TOTAL_TESTS_ERROR=0
```

## Segurança

```text
SIMULATION_ONLY=YES
REAL_MONEY_ENABLED=NO
REAL_ORDER_ENABLED=NO
AUTO_CLICK_ENABLED=NO
BROKER_LOGIN_AUTOMATION=NO
CREDENTIALS_ALLOWED=NO
CAPTURE_EXECUTED=NO
OCR_EXECUTED=NO
BROKER_OPENED=NO
SERVER_STARTED=NO
REAL_RUNTIME_CHANGED=NO
FUNCTIONAL_CODE_CHANGE=NO
```

As ressalvas visuais da PTP-GOV.4.5 permanecem adiadas e nenhuma etapa PTP-GOV.4.6B ou
posterior foi iniciada.
