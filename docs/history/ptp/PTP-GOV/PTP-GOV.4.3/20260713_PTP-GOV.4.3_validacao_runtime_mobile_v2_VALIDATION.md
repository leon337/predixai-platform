# PTP-GOV.4.3 — Validação runtime controlada da Mobile V2

Data: 2026-07-13  
Base: `eb11c9f28dcb596dc2b9414a4cebc16867e10a53`  
Status: `PASS_FINAL_CONTROLLED_RUNTIME_VALIDATION`

## Resultado

A Mobile V2 foi validada em runtime real local, exclusivamente em `127.0.0.1:5001`, por
uma instância criada e encerrada pelo próprio validador. Todo estado, lock e backup da
instância ficaram dentro de um único `TemporaryDirectory`; `data/runtime/` foi apenas
comparado por hash e permaneceu inalterado.

## Processo controlado

- classificação inicial: `PORT_5001_FREE`;
- PID/PGID próprio: `18501` / `18501`;
- encerramento: `SIGTERM` somente no grupo próprio;
- `SIGKILL_USED=NO`;
- classificação final: `PORT_5001_FREE`;
- `RESIDUAL_PROCESS_COUNT=0`.

Nenhum processo externo foi encerrado e não houve fallback de porta.

## HTTP local

As oito operações retornaram HTTP 200:

1. `GET /`;
2. `GET /health` com `application_id=MOBILE_V2`;
3. `GET /session/setup`;
4. `POST /session/create` com payload simulado;
5. `GET /mobile`;
6. `GET /state/current`;
7. `POST /observer/start`;
8. `POST /observer/stop`.

Os payloads de estado preservaram `simulation_only=true`, ordens, dinheiro real e
auto-click desabilitados.

## Backup e recovery

`create_backup()` e `recover_from_backup()` foram exercitados somente no diretório
temporário. Criação, schema, recuperação explícita e igualdade do estado restaurado
passaram. Um backup com schema inválido gerou erro controlado e não substituiu o estado
válido.

## Testes

```text
PRE_TESTS_PASS=41
POST_TESTS_PASS=41
EXISTING_TESTS_PASS=17
CURRENT_CHARACTERIZATION_TESTS_PASS=21
FUNCTIONAL_CONTRACTS_PASS=3
VALIDATOR_TESTS_PASS=6
TOTAL_TESTS_FAIL=0
TOTAL_TESTS_ERROR=0
```

Dois ajustes feitos durante a etapa ficaram restritos ao validador: endurecimento literal
da allowlist `127.0.0.1:5001` e registro correto de processo não iniciado em bloqueio de
sandbox. Nenhum código funcional foi alterado.

## Segurança e integridade

```text
REAL_RUNTIME_USED=NO
REAL_RUNTIME_CHANGED=NO
EXTERNAL_NETWORK_USED=NO
LEGACY_STARTED=NO
DASHBOARD_STARTED=NO
BROKER_OPENED=NO
REAL_MONEY_ENABLED=NO
REAL_ORDER_ENABLED=NO
AUTO_CLICK_ENABLED=NO
BROKER_LOGIN_AUTOMATION=NO
FUNCTIONAL_FIX_APPLIED=NO
```

O dirty preexistente permaneceu fora da allowlist. Nenhuma próxima PTP foi iniciada.
