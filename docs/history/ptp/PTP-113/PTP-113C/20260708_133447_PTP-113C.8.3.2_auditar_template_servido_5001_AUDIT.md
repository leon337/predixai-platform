# PTP-113C.8.3.2 — Auditoria do Template Servido e Processo 5001

## 1. Identificação
- PTP: PTP-113C.8.3.2
- Nome curto: auditar_template_servido_5001
- Data: 2026-07-08 13:34:48 -0300
- Status: PASS_AUDIT
- Relatório TXT: `reports/20260708_133447_PTP-113C.8.3.2_auditar_template_servido_5001_AUDIT.txt`
- Base URL: `http://127.0.0.1:5001`

## 2. Objetivo

Descobrir por que o template local alterado na C.8.3 não apareceu no HTML servido por `/mobile`.

## 3. Contexto

A C.8.3 falhou com rollback seguro. O template local recebeu o bloco temporariamente, mas o processo Flask ativo em `http://127.0.0.1:5001` não serviu o card novo.

## 4. Arquivos afetados

Arquivos auditados e protegidos:

```txt
src/predixai/mobile_v2/app.py src/predixai/mobile_v2/state_store.py src/predixai/mobile_v2/templates/session_setup.html src/predixai/mobile_v2/templates/mobile_v2_index.html
```

Arquivos gerados:

```txt
reports/20260708_133447_PTP-113C.8.3.2_auditar_template_servido_5001_AUDIT.txt
docs/history/ptp/PTP-113/PTP-113C/20260708_133447_PTP-113C.8.3.2_auditar_template_servido_5001_AUDIT.md
```

## 5. Execução

A auditoria verificou:

- git status e último commit;
- PID escutando na porta 5001;
- comando, cwd e env filtrado do processo;
- HTTP de `/health`, `/session/setup`, `/mobile`, `/state/current`;
- rotas e `render_template` em `app.py`;
- template local versus HTML servido;
- `session.contract` em `/state/current`;
- checksums antes/depois dos arquivos protegidos.

## 6. Validação

Resultado final:

```txt
PASS_AUDIT
Auditoria executada sem alteração em arquivos protegidos.
RESTART_CONTROLADO_PROVAVELMENTE_NECESSARIO_ANTES_DE_VALIDAR_TEMPLATE
```

## 7. Resultado

`PASS_AUDIT`

## 8. Decisões

- Não reabrir C.8.3 nesta etapa.
- Não alterar template, app.py ou state_store.py.
- Usar o diagnóstico desta auditoria para decidir se a próxima etapa será restart controlado, correção de processo/caminho ou nova validação visual.

## 9. Pendências

- Leo colar/analisar o relatório TXT no chat.
- Confirmar se o processo 5001 está rodando no repo esperado.
- Decidir próxima mini-PTP conforme resultado.

## 10. Próxima ação

Analisar `reports/20260708_133447_PTP-113C.8.3.2_auditar_template_servido_5001_AUDIT.txt` e definir se a próxima etapa será:

```txt
C.8.3.3 — Restart controlado + revalidação do template servido
```

ou correção prévia do processo/porta/template.
