# PTP 113 C.4 — Fechamento Simulado WIN/LOSS/DRAW

## 1. Identificação

- PTP: PTP 113 C.4
- Nome curto: fechamento_simulado_win_loss_draw
- Data: 2026-07-07 10:21:22 -0300
- Status: FIX
- Relatório TXT: `reports/20260707_102122_PTP-113C.4_fechamento_simulado_win_loss_draw_FIX.txt`

## 2. Objetivo

Criar uma camada segura de fechamento simulado para calcular WIN/LOSS/DRAW sem alterar saldo.

## 3. Contexto

A PTP 113 C.3 criou `simulated_open_operation`. A PTP 113 C.4 cria `simulated_close_result`, mantendo a atualização da banca bloqueada para a PTP 113 C.5.

## 4. Arquivos afetados

```txt
scripts/predixai_trader_mobile_server.py
docs/reports_index.md
reports/
docs/history/ptp/PTP-113/PTP-113C/
```

## 5. Execução

Foi adicionado o bloco:

```txt
PTP113C4_FECHAMENTO_SIMULADO_RESULTADO
```

O endpoint `/api/mobile/state` passa a expor:

```txt
simulated_close_result
ptp113c4_simulated_close_result_ready
```

## 6. Validação

```txt
python -m py_compile
git diff --check
curl /mobile
curl /api/mobile/state
validação de simulated_close_result
validação das travas simuladas
```

## 7. Resultado

```txt
FIX
```

## 8. Decisões

- Não alterar saldo nesta etapa.
- Não atualizar banca nesta etapa.
- Não calcular resultado sem operação aberta.
- Não calcular resultado sem preço de fechamento válido.
- Preservar travas simuladas.

## 9. Pendências

- Leo colar relatório no chat.
- Validar se fechamento fica bloqueado sem operação aberta.
- Debater PTP 113 C.5 — Atualização da Banca Simulada.

## 10. Próxima ação

```txt
PTP 113 C.5 — Atualização da Banca Simulada
```
