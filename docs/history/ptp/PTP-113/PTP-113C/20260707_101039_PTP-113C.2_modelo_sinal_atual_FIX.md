# PTP 113 C.2 — Modelo do Sinal Atual

## 1. Identificação

- PTP: PTP 113 C.2
- Nome curto: modelo_sinal_atual
- Data: 2026-07-07 10:10:39 -0300
- Status: FIX
- Relatório TXT: `reports/20260707_101039_PTP-113C.2_modelo_sinal_atual_FIX.txt`

## 2. Objetivo

Criar uma camada única de modelo do sinal atual para impedir que dado histórico antigo seja usado como sinal operacional.

## 3. Contexto

A auditoria da PTP 113 C.1 mostrou que o endpoint ainda carregava histórico antigo dentro de campos de estado atual. Esta etapa cria um normalizador seguro antes de abrir operação simulada.

## 4. Arquivos afetados

```txt
scripts/predixai_trader_mobile_server.py
docs/reports_index.md
reports/
docs/history/ptp/PTP-113/PTP-113C/
```

## 5. Execução

Foi adicionado o bloco:

```txt
PTP113C2_MODELO_SINAL_ATUAL
```

O bloco injeta em `/api/mobile/state`:

```txt
current_signal_model
ptp113c2_current_signal_model_ready
```

## 6. Validação

Validações executadas:

```txt
python -m py_compile
git diff --check
curl /mobile
curl /api/mobile/state
validação de current_signal_model
validação das travas simuladas
```

## 7. Resultado

```txt
FIX
```

## 8. Decisões

- Não abrir operação nesta etapa.
- Não calcular resultado nesta etapa.
- Não alterar saldo nesta etapa.
- Bloquear uso operacional de histórico antigo.
- Reaproveitar estado existente em vez de duplicar motor de sinais.

## 9. Pendências

- Leo validar relatório no chat.
- Debater PTP 113 C.3 — Abertura Simulada da Operação.

## 10. Próxima ação

```txt
PTP 113 C.3 — Abertura Simulada da Operação
```
