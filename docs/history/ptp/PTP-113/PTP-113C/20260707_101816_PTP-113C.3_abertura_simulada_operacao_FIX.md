# PTP 113 C.3 — Abertura Simulada da Operação

## 1. Identificação

- PTP: PTP 113 C.3
- Nome curto: abertura_simulada_operacao
- Data: 2026-07-07 10:18:16 -0300
- Status: FIX
- Relatório TXT: `reports/20260707_101816_PTP-113C.3_abertura_simulada_operacao_FIX.txt`

## 2. Objetivo

Criar uma camada segura de abertura simulada da operação, usando `current_signal_model` como trava.

## 3. Contexto

A PTP 113 C.2 criou `current_signal_model` para impedir que histórico antigo vire sinal atual. A PTP 113 C.3 usa esse modelo para montar uma operação simulada apenas quando houver sinal atual válido.

## 4. Arquivos afetados

```txt
scripts/predixai_trader_mobile_server.py
docs/reports_index.md
reports/
docs/history/ptp/PTP-113/PTP-113C/
```

## 5. Execução

Foi adicionado o bloco:

```txt
PTP113C3_ABERTURA_SIMULADA_OPERACAO
```

O endpoint `/api/mobile/state` passa a expor:

```txt
simulated_open_operation
ptp113c3_simulated_open_operation_ready
```

## 6. Validação

```txt
python -m py_compile
git diff --check
curl /mobile
curl /api/mobile/state
validação de simulated_open_operation
validação das travas simuladas
```

## 7. Resultado

```txt
FIX
```

## 8. Decisões

- Não calcular resultado nesta etapa.
- Não alterar saldo nesta etapa.
- Não abrir operação se `current_signal_model.operation_allowed=false`.
- Não executar ordem real.
- Preservar travas simuladas.

## 9. Pendências

- Leo colar relatório no chat.
- Validar se a operação fica bloqueada com observador desligado.
- Debater PTP 113 C.4 — Fechamento Simulado WIN/LOSS/DRAW.

## 10. Próxima ação

```txt
PTP 113 C.4 — Fechamento Simulado WIN/LOSS/DRAW
```
