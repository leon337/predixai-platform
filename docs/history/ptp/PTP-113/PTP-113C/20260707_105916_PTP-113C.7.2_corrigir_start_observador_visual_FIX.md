# PTP 113 C.7.2 — Corrigir Start do Observador Visual

## 1. Identificação

- PTP: PTP 113 C.7.2
- Nome curto: corrigir_start_observador_visual
- Data: 2026-07-07 10:59:16 -0300
- Status: FIX
- Relatório TXT: `reports/20260707_105916_PTP-113C.7.2_corrigir_start_observador_visual_FIX.txt`

## 2. Objetivo

Corrigir o botão **Iniciar simulado** para também acionar o reader/observador visual.

## 3. Contexto

O teste prático com a corretora aberta mostrou que o mobile permanecia OFF/PARADO mesmo após o comando de iniciar. A causa provável era o botão iniciar sessão simulada sem ligar o reader visual.

## 4. Arquivos afetados

```txt
scripts/predixai_trader_mobile_server.py
docs/reports_index.md
reports/
docs/history/ptp/PTP-113/PTP-113C/
```

## 5. Execução

Foram adicionados:

```txt
/api/mobile/observer/start
/api/mobile/observer/stop
/api/mobile/observer/status
```

E o botão mobile **Iniciar simulado** passou a chamar o start do observador visual.

## 6. Validação

```txt
python -m py_compile
git diff --check
curl /mobile
grep marcador no HTML real
curl /api/mobile/observer/status
POST /api/mobile/observer/start
curl /api/mobile/state
validação das travas simuladas
```

## 7. Resultado

```txt
FIX
```

## 8. Decisões

- Não executar ordem real.
- Não usar saldo real.
- Não clicar em compra/venda.
- Não ativar auto-click.
- Corrigir apenas o acionamento do observador visual.

## 9. Pendências

- Leo validar no celular com a corretora aberta.
- Enviar prints do Observador ON/READING ou mensagem de erro real.

## 10. Próxima ação

```txt
PTP 113 C.7.3 — Validar Observador ON com Fonte Visual
```
