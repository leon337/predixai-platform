# PTP-113B.3.1B.3 — Corrigir Contrato Real Mobile

## 1. Identificação

- PTP: PTP-113B.3.1B.3
- Nome curto: contrato_real_mobile
- Data: seg 06 jul 2026 23:58:28 -03
- Status: PASS
- Relatório TXT: `/home/leo/Documentos/GitHub/predixai-platform/reports/20260706_235822_PTP-113B.3.1B.3_contrato_real_mobile_FIX.txt`
- Arquivo principal: `scripts/predixai_trader_mobile_server.py`

## 2. Objetivo

Corrigir a tela `/mobile` para carregar corretamente os dados reais do contrato configurado em `/session/setup`.

## 3. Contexto

Após a etapa `PTP-113B.3.1B.2`, a tela passou a exibir o card de contrato, mas mostrou dados incorretos: banca inicial como `{}`, recuperação como JSON bruto, saldo/entrada antigos e overflow no celular.

## 4. Arquivos afetados

- `scripts/predixai_trader_mobile_server.py`
- `reports/`
- `docs/history/ptp/PTP-113/PTP-113B/`
- `docs/reports_index.md`

## 5. Execução

O script:

1. auditou o JSON real de `/api/mobile/state`;
2. criou backup técnico fora do Git;
3. adicionou patch frontend com prioridade para `mobile_session.contract`;
4. formatou valores BRL;
5. formatou recuperação como texto curto;
6. corrigiu labels de modo e estratégia;
7. reduziu overflow horizontal;
8. ocultou gráfico antigo duplicado quando detectado;
9. reiniciou servidor mobile;
10. validou HTTP e segurança simulada.

## 6. Validação

- `python3 -m py_compile scripts/predixai_trader_mobile_server.py`
- `/mobile`
- `/api/mobile/state`
- marcadores do patch
- travas simuladas
- `git diff --check`

## 7. Resultado

PASS.

## 8. Pendências

Leo deve validar visualmente no celular se:

- banca inicial mostra valor BRL;
- entrada mostra valor configurado;
- recuperação aparece como texto curto;
- não existe JSON bruto;
- não existe barra horizontal exagerada;
- gráfico duplicado foi removido/ocultado.

## 9. Próxima ação

Se o visual estiver correto, registrar PASS visual da PTP-113B.3.1B.3. Se ainda houver divergência de valores, auditar gravação da primeira tela `/session/setup`.
