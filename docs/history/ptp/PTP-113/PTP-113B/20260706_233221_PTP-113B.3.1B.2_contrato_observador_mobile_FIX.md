# PTP-113B.3.1B.2 — Observador Contract UX

## 1. Identificação

- PTP: PTP-113B.3.1B.2
- Nome curto: contrato_observador_mobile
- Data: seg 06 jul 2026 23:32:26 -03
- Status: WARN
- Relatório TXT: `/home/leo/Documentos/GitHub/predixai-platform/reports/20260706_233221_PTP-113B.3.1B.2_contrato_observador_mobile_FIX.txt`
- Arquivo principal: `scripts/predixai_trader_mobile_server.py`

## 2. Objetivo

Carregar na tela `/mobile` o conteúdo principal do contrato configurado na primeira tela `/session/setup`, incluindo saldo, banca, entrada, meta, recuperação, exposição e modo operacional. Também adicionar gráfico vivo e feedback visual nos botões.

## 3. Contexto

Após a auditoria `PTP-113B.3.1B.1`, foi confirmado que o backend já possui vários dados do contrato, mas a segunda tela ainda não os apresenta de forma operacional clara.

## 4. Arquivos afetados

- `scripts/predixai_trader_mobile_server.py`
- `reports/`
- `docs/history/ptp/PTP-113/PTP-113B/`
- `docs/reports_index.md`
- `backups/ptp/PTP-113/PTP-113B/`

## 5. Execução

O script:

1. criou backup do servidor mobile;
2. injetou card de contrato da sessão na tela `/mobile`;
3. adicionou gráfico vivo com canvas;
4. adicionou atualização por `fetch('/api/mobile/state')`;
5. adicionou feedback visual nos botões;
6. validou `py_compile`;
7. reiniciou o servidor mobile;
8. validou endpoints HTTP;
9. preservou travas simuladas.

## 6. Validação

- `python3 -m py_compile scripts/predixai_trader_mobile_server.py`
- validação de marcador UX no código
- validação de card de contrato
- validação de gráfico vivo
- validação de feedback visual dos botões
- `/mobile`
- `/api/mobile/state`
- `git diff --check`

## 7. Resultado

WARN.

## 8. Decisões

- A tela observador passa a carregar visualmente o contrato da primeira tela.
- Os botões passam a ter resposta visual para indicar clique/processamento.
- O gráfico passa a ter área viva, mesmo quando ainda está coletando poucos dados.
- Tudo permanece 100% simulado.

## 9. Pendências

- Leo deve validar visualmente no navegador/celular.
- Se o card não aparecer, limpar cache ou reiniciar manualmente o servidor.
- Após PASS visual, avançar para validação do ciclo observador prático.

## 10. Próxima ação

Leo deve abrir `/mobile`, testar os botões e confirmar se saldo, contrato e gráfico aparecem corretamente.
